# common-schemas

Status: Not implemented (Architecture Phase)

Single source of truth for cross-service data contracts: request and response
shapes, internal messages between gateway, scheduler, and adapter, and typed
error definitions. Services depend on this package rather than redefining shapes.

See `docs/architecture/system-overview.md`. No code exists yet.
